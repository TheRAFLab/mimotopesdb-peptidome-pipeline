[project]
name = "mimotopesdb-peptidome-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.14"
dependencies = [
    "polars>=1.43.1",
    "rich>=15.0.0",
]

[dependency-groups]
dev = [
    "duckdb>=1.5.5",
    "pytest>=9.1.1",
]
